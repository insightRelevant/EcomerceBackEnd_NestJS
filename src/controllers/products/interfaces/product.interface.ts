
 
export interface Product { //here we defined a interface of Product and his typesof data. that will be a JSON 
    id : number,
    name : string,
    description : string,
    price  : number,
    tags: string[];
}
